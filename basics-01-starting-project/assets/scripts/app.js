let currentResult = 0;
currentResult = currentResult + 5;
// template literal is the `${variable}`
let currentResultOperation = `0 + ${currentResult}`
outputResult(currentResult, currentResultOperation);
